# TinySearch roadmap

- Add provider-specific adapters and exact schemas to the shared catalog.
- Validate each provider against deterministic HTTP fixtures.
- Verify live provider behavior with separately supplied credentials.
